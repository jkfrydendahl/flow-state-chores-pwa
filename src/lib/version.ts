// Release identifier: YY.M.D.N, where N starts at 1 each release date.
// Increment N for another release on the same date; use the release date otherwise.
// Keep stable across rebuilds so a cached app reports its actual source version.
export const appVersion = "26.9.10.1";
